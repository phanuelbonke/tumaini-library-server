import React from "react";

function PublisherDetails(props) {
    return (
        <div>
            <h1>Publisher {props.PublisherId} Details</h1>
        </div>
    );
}

export default PublisherDetails;